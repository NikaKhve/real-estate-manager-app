import RedberryIcon from "./icons/RedberryIcon";
import classes from "./Header.module.scss";

const Header = () => {
  return (
    <div className={classes.container}>
      <RedberryIcon />
    </div>
  );
};

export default Header;
