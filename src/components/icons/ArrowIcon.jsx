import right from "@/assets/right.svg";

const ArrowIcon = () => {
  return (
    <img
      src={right}
      height="28px"
      alt="right-icon"
      style={{ cursor: "pointer" }}
    />
  );
};

export default ArrowIcon;
